d.connect(lambda: self.clicked("do't touch me! xiu huo."))
        # self.actionSave.triggered.connect(lambda: self.clicked("do't touch me! xiu huo."))
		# self.actionPrase.triggered.connect(lambda: self.clicked("do't touch me! xiu huo."))
        # self.actionCopy.triggered.connect(